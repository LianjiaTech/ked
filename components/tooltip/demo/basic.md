---
order: 0
title: 基本
---

最简单的用法。

```jsx
import { Tooltip } from '@ke/ked'

ReactDOM.render(
  <Tooltip title="prompt text">
    <span>Tooltip will show when mouse enter.</span>
  </Tooltip>,
  mountNode,
)
```
